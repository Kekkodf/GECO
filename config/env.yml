name: geco
channels:
  - conda-forge
  - defaults
dependencies:
  - python 3.12
  - pip
  - pip:
      - numpy
      - pandas
      - matplotlib
      - seaborn
      - tqdm
      - ir-measures
      - ir-datasets
      - torch
      - transformers
      - pingouin
      - faiss_cpu
name: geco
channels:
  - conda-forge
  - defaults
dependencies:
  - python 3.12
  - pip
  - pip:
      - numpy
      - pandas
      - matplotlib
      - seaborn
      - logging
      - tqdm
      - ir-measures
      - ir-datasets
      - torch
      - transformers==4.49.0
      - pingouin
      - faiss_cpu
      - vec2text
      - tabulate